package dev.bookreports.config;

/**
 * Where {@link UpdateCheckerSettings#resource()} identifies a project — see
 * {@code dev.bookreports.update.UpdateChecker}.
 */
public enum UpdateSource {
    GITHUB, MODRINTH
}
